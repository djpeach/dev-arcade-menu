let config =  {
  gamesDir: '../dev-arcade-games/',
  messageTypes: {
    keyDown: 'KEY_DOWN',
    keyUp: 'KEY_UP',
    gameOver: 'GAME_OVER'
  }
}

export const init = (ops) => {

}

export default config